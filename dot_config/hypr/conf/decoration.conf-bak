# See https://wiki.hyprland.org/Configuring/Variables/ for more
decoration {
    rounding = 3
    blur {
        enabled = true
        size = 3
        passes = 1
    }
    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
}

# windowrule = opacity 0.1, class:rofi
# Assombrir uniquement
# Apply dimming to Rofi
# windowrulev2 = dimaround, class:(rofi)
